global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'rails'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['web:3000']
    basic_auth:
      username: admin
      password: secure_metrics_password 